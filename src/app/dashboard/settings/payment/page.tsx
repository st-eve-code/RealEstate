'use client'

import EnhancedSettings from '@/components/Admin/Settings/EnhancedSettings';

export default function PaymentSettingsPage() {
  return <EnhancedSettings isSidebarCollapsed={false} activeTab="payment" />;
}
