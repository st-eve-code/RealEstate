'use client'

import EnhancedSettings from '@/components/Admin/Settings/EnhancedSettings';

export default function NotificationSettingsPage() {
  return <EnhancedSettings isSidebarCollapsed={false} activeTab="notification" />;
}
