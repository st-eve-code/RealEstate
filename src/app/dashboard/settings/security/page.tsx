'use client'

import EnhancedSettings from '@/components/Admin/Settings/EnhancedSettings';

export default function SecuritySettingsPage() {
  return <EnhancedSettings isSidebarCollapsed={false} activeTab="security" />;
}
